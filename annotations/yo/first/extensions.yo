Before we continue with the `real' object-approach to programming, we first
introduce some notable differences with the bf(C) programming language: not
mere differences between bf(C) and bf(C++), but important syntactic constructs
and keywords not found or differently used in bf(C).
