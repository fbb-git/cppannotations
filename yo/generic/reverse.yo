hi(reverse())
itemization(
    it() Header file:
    centt(#include <algorithm>)
    it() Function prototype:
    itemization(
        itt(void reverse(BidirectionalIterator first,
            BidirectionalIterator last);)
    )
    it() Description:
    itemization(
        it() The elements in the range pointed to by rangett(first, last)
are reversed.
    )
    it() Example:
    verbinclude(generic/examples/reverse.cc)
)
