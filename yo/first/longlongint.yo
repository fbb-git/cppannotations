The C++11 standard adds the type ti(long long int) to the set of standard
types. On 32 bit systems it has at least 64 usable bits. Some compilers
already supported tt(long long int) as an extension, but C++11 officially
adds it to bf(C++).
