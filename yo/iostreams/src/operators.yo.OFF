
The insertion and extraction operators are used to write information to or
read information from, respectively, em(ostream) and em(istream) objects (and
to all classes derived from these classes). By default, em(white space) is
skipped when the insertion and extraction operators are used.
