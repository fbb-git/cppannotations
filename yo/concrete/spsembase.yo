The parser uses tt(spSemBase) as its semantic value. The class tt(spSemBase)
is a wrapper around tt(std::shared_ptr<SemBase>), offering a constructor
member template which must be given a pointer to a dynamically allocated
tt(Semantic) object. Its interface is all that is required:
        verbinclude(poly2/spsembase/spsembase.h)
