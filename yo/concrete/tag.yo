Our program handles three types of semantic values: numbers, text, and
vectors of semantic values, which are either numbers or text. These distinct
types are indicated by em(tag) enumeration values:
        verbinsert(TAG)(poly2/sembase/sembase.h)

